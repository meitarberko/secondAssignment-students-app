<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <!-- LAUNCHER / Default screen -->
        <activity
            android:name=".ui.list.StudentsListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other screens -->
        <activity android:name=".ui.details.StudentDetailsActivity" />
        <activity android:name=".ui.form.EditStudentActivity" />
        <activity android:name=".ui.form.NewStudentActivity" />

        <activity android:name=".MainActivity" />

    </application>

</manifest>
